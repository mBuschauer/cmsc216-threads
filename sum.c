#include "sum.h"

int sum(int *array, int size, int num_threads) {
    /*
            You need to correctly compute the sum of elements in the array based on the following 
            formula: sum = (sum + a[i]) % 1000000, where a[i] represents an array element.
    */
    return 0;
}